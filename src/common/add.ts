// This is an example of how we can write tests
// in separate `/tests` project.

// See `/tests/main.test.ts` for an example set of tests.
export default function add(a: number, b: number): number {
  return a + b;
}
